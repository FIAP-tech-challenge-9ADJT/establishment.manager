package tech.challenge.establishment.manager.Service;

import org.springframework.stereotype.Service;

import java.time.Instant;
import java.util.UUID;

@Service
public class AddressService {

    /*

    private AddressRepository addressRepository;

    public AddressService(AddressRepository addressRepository){
        this.addressRepository = addressRepository;
    }

    public UUID createAddress(CreateAddressDTO createAddressDTO){
        var entity = new Address(null, createAddressDTO.street()...);
        return addressRepository.save(entity).getId();
    }

    */

}
